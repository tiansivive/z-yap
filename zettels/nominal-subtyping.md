---
tags: [concept, type-system, mechanism]
---
# Nominal Subtyping

A subtyping discipline where compatibility requires an explicit declared relationship (extends, implements). Two types with identical structure are unrelated unless one declares the other as a supertype.

Yap has no subtyping of any kind — neither structural nor nominal. This zettel exists for contrast and reference.
