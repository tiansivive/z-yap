#!/bin/sh
# Generate derived artifacts before commit.
# Install: git config core.hooksPath scripts/hooks

ROOT="$(cd "$(dirname "$0")/../.." && pwd)"

node "$ROOT/scripts/threads.js" --html
node "$ROOT/scripts/threads.js" --obsidian
node "$ROOT/scripts/embed-connections.js"
